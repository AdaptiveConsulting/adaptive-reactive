using NUnit.Framework;

namespace Adaptive.Observables.Tests
{
    [TestFixture]
    public class ObservableDictionaryTests
    {
        /*Dictionary should yield inserted element as inserted 
Dictionary should yield updated element as updated and call update function 
Dictionary should yield replaced element as replaced 
Dictionary should yield existing element as existing 
Dictionary should yield removed element as removed 
Dictionary should yield cleared element as cleared 
Dictionary keyed stream should yield inserted element as inserted 
Dictionary keyed stream should yield updated element as updated and call update function 
Dictionary keyed stream should yield replaced element as replaced 
Dictionary keyed stream should yield existing element as existing 
Dictionary keyed stream should yield removed element as removed 
Dictionary keyed stream should yield cleared element as cleared */
    }
}